services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taxi_dashboard
    env_file:
      - .env
    ports:
      - "8501:8501"
    restart: unless-stopped
